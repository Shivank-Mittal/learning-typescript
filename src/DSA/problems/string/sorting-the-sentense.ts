export function sortTheString(str: string) {

    sort(str.split(''))
}

function sort(strArray: string[]){
    const mid = Math.floor(strArray.length/2);
    const leftA = strArray.slice(0, mid);
    const leftB = strArray.slice(mid  + 1);
    
    // if(leftA.length === 1 || leftB.length === 1 ) {
    //     const sortedArray = 
    //     while 
    // }
 }
